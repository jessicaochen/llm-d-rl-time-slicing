apiVersion: v1
kind: Service
metadata:
  name: {{ include "timesliceorchestrator.fullname" . }}
  namespace: timeslice-system
  labels:
    {{- include "timesliceorchestrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
    - port: 8080
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "timesliceorchestrator.selectorLabels" . | nindent 4 }}
